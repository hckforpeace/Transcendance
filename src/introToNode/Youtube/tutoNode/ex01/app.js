
// // Global object in Javascript
// console.log(); // global

// setTimeout(); // works also in node
// clearTimeout();

// setInterval();
// clearInterval();


// this vraiable is not in the global object they are only know in the scope of the file app.js
var message = '';
// console.log(global.message); OUTPUT is Undefined

// In node this doesn't work the global window doesn't exist
// window.message;

// One used globals in NODE are added to the object global

